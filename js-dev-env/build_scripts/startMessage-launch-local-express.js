var path = require('path');
var chalk = require('chalk');
console.log(chalk.green('############################'));
console.log(chalk.green('Starting localhost ...'));
console.log(chalk.green('Go to http://localhost:3000'));
console.log(chalk.green('############################'));
